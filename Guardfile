# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch('views/index.haml')
  watch(%r{public/css/.+\.(css)})
  watch(%r{public/scripts/.+\.(js)})
end

guard :compass
